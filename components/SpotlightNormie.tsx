"use client";
// SpotlightNormie is unused in the new design (homepage handles its own spotlight).
// Kept as a stub to avoid import errors.
export default function SpotlightNormie({ tokenId }: { tokenId: number }) {
  return null;
}
